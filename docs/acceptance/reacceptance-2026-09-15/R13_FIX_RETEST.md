# r13 修复与复验（2026-09-17）

本轮针对用户要求“修复后重新打包”。旧r12交付物保留，候选构建使用原构建目录；修改前代码已保存至 work/reacceptance-2026-09-15/source-r12-code-frozen（只含代码/资源和顶层测试源码，不复制求解输出）。

## 功能验收表与缺陷台账增量

|问题|处理|本轮证据/边界|
|---|---|---|
|SMV-STARTUP-01 结果窗口迅速退出|普通打开不再附加配置脚本；烟火使用原生加载参数；切片/粒子使用加载脚本并以NOEXIT保持运行。移除不稳定的LOADINIFILE初始化；独立空标准输入、隐藏控制台及输出/退出记录|r13-production-launch-final：生产SmokeviewLauncher.cpp单独编译驱动同包Smokeview，普通/烟火/切片/粒子各保持20秒，随后只关闭自有子进程。之前失败测试保留，不冒充全部播放或嵌入交互验收|
|RECOVERY-01 旧恢复副本反复提示|中文正文和明确按钮；可保留本次全部待处理副本并停止提醒，开始页仍可恢复，新副本仍提醒；同一窗口成功生成新副本后替换上一份；处理失败反馈|Core及A15可靠性回归通过，包括确认标记重启保留、恢复文件可读、越界路径拒绝。未修改用户真实恢复文件|
|UI-03 校验消息过小、位置异常|继承r12自适应尺寸、内容换行和居中修复|最终LayoutTutorial及UiFeedback回归通过；r12已有真实桌面限定证据。全部DPI/多显示器组合尚未全验|
|UI-04 浮动工作区检查器过小、靠边|继承r12浮动尺寸及居中、保留有效用户布局修复|最终布局回归通过，涵盖保存/恢复及窗口析构；同上保留显示环境边界|
|NV-04/NV-05 显示默认值|字体放大及按数据范围取切片色标写入随包smokeview.ini，避免配置启动脚本；用户工程INI仍优先|最终4入口使用此配置；不宣称所有色标/DPI视觉状态通过|

## 验证

- r13-build-03：Release构建成功，构建期间输入保持一致。
- r13-final-regressions：Core、UiLanguage、UiFeedback、A15Reliability、NativeViewer、LayoutTutorial、P26GuidedTutorial共7/7通过，执行期间绑定保持一致。
- r13-production-launch-06：普通/烟火/切片各20秒通过；r13-production-launch-final追加最终配置及粒子，4/4通过。主动关闭产生的退出码不能当作崩溃。
- 离线SmokeviewFrameRenderer未修改，交互脚本NOEXIT不会施加到离线渲染脚本。
- 安装/卸载和交付文件校验结果另见 r13-trial-delivery/REPORT.md（完成后生成）。

最初仅LOADINIFILE脚本会以0快速结束；仅增加NOEXIT出现0xc0000005，增加时间命令后仍有失败；普通直接QProcess也曾间歇失败。最终方案避开初始化脚本并调整子进程输入/输出和Windows启动方式。未用单一原因解释所有历史异常，未隐藏中间失败。

## 未扩大结论的事项

完整111项人工验收、完整教学视频、PyroSim授权对照、第二台Windows验证仍未完成。NV-02宿主缩放仍采用r12禁用误导按钮及原生操作指引，不称新增原生缩放接口；NV-03仍沿用r12播放切换方案，不称已实现双向原生状态协议。本轮交付是修复试用包，不是所有历史宽功能行最终通过。
